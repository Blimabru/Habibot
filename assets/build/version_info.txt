# UTF-8
# Arquivo de version info do Windows para PyInstaller (--version-file)
# Referência: https://pyinstaller.org/

VSVersionInfo(
  # Bloco principal de informações de versão do executável
  ffi=FixedFileInfo(
    filevers=(1, 1, 0, 0),         # Versão do arquivo (major, minor, patch)
    prodvers=(1, 1, 0, 0),         # Versão do produto (major, minor, patch)
    mask=0x3F,                     # Máscara de campos válidos
    flags=0x0,                     # Flags de status
    OS=0x40004,                    # Tipo de sistema operacional (Windows NT 32-bit)
    fileType=0x1,                  # Tipo de arquivo (aplicativo)
    subtype=0x0,                   # Subtipo (não usado)
    date=(0, 0)                    # Data (não usado)
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          '041604B0',
          [
            # Metadados exibidos nas propriedades do executável
            StringStruct('CompanyName', 'Bruno Lima'),  # Nome do autor/empresa
            StringStruct('FileDescription', 'Este bot de automação foi criado para suprir a necessidade de um relatório completo no Sistema de Habitação que disponibilize todos os dados dos candidatos cadastrados.'),
            StringStruct('FileVersion', '1.1.0'),        # Versão do arquivo
            StringStruct('InternalName', 'Habibot'), # Nome interno
            StringStruct('LegalCopyright', '© 2026 Bruno Lima'),
            StringStruct('OriginalFilename', 'Habibot.exe'), # Nome do arquivo gerado
            StringStruct('ProductName', 'Bot de Automação Habibot'), # Nome do produto
            StringStruct('ProductVersion', '1.1.0'),     # Versão do produto
            StringStruct('Comments', 'Idioma: Português Brasileiro') # Comentários adicionais
          ]
        )
      ]
    ),
    # Informação de localização (idioma e codepage)
    VarFileInfo([VarStruct('Translation', [0x0416, 1200])])
  ]
)